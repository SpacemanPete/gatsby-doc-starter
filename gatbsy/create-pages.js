'use strict'

const path = require('path')

const createPages = ({ actions, graphql }) => {
    const { createPage } = actions

    // return graphql call
    // then
    // check for errors
    // build nav object
    // run function to create relevant pages






}

module.exports = createPages